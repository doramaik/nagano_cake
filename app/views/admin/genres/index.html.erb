<%= form_with model: @genre, url: '/admin/genres',method: :post do |f| %>
  <h4>Title</h4>
  <%= f.text_field :name %>

  <%= f.submit 'Create Book' %>
  </div>

<% end %>

<% @genres.each do |genre| %>
<%= genre.name %>
<% end %>